#include "Logger.h"
 
int main()
{
	// ProductPool my_pool;
	// Console con;
	// Production pr(&my_pool, &con, 3);
    // Production pr2(&my_pool, &con, 5);
    // Consumer cons(&my_pool,&con,5);
    // Consumer cons2(&my_pool,&con,9);

	Logger logger;
	logger.log("lalala");

	return 0;
} 